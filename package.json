{
  "name": "agent-detection",
  "version": "1.0.0",
  "description": "Detects current user agent and adds in case a classname to the html DOM tag",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec",
    "build": "mkdir -p dist/js && uglifyjs src/*.js -m -o dist/js/app.js && uglifyjs src/*.js -m -c -o dist/js/app.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git@repos.codehanzo.com:hanzo/agent-detection.git"
  },
  "keywords": [
    "user",
    "agent",
    "detection"
  ],
  "author": "Hanzo",
  "license": "ISC",
  "devDependencies": {
    "jsdom": "9.12.0",
    "jsdom-global": "2.1.1",
    "mocha": "^3.3.0",
    "uglify-js": "^2.8.22"
  }
}
